% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{validate_msd}
\alias{validate_msd}
\title{Validate mean squared displacement}
\usage{
validate_msd(
  observed,
  simulated,
  max_lag = NULL,
  envelope_probs = c(0.025, 0.975),
  ...
)
}
\arguments{
\item{observed}{Observed track-like object.}

\item{simulated}{A list of simulated track-like objects or a data frame with
a simulation identifier column.}

\item{max_lag}{Optional maximum lag. If \code{NULL}, the largest common lag across
observed and simulated tracks is used.}

\item{envelope_probs}{Numeric vector of length two giving lower and upper
envelope quantiles.}

\item{...}{Passed to \code{\link[=as_gmov_simulations]{as_gmov_simulations()}}.}
}
\value{
A list with MSD curves, simulation envelope, discrepancy statistics,
and a Monte Carlo rank test.
}
\description{
Compares the observed mean squared displacement curve with the envelope and
expected curve from simulated trajectories. The discrepancy statistic is the
integrated squared error between the observed MSD curve and the mean
simulated MSD curve.
}
\examples{
observed <- data.frame(x = 0:4, y = 0)
simulated <- list(
  data.frame(x = 0:4, y = 0),
  data.frame(x = 0:4, y = c(0, 0, 1, 1, 1))
)
validate_msd(observed, simulated, max_lag = 2)
}
\references{
Nicosia, A. (2026). Beyond the next step: A multi-criteria generative
validation framework for step selection functions. \emph{Methods in Ecology and
Evolution}. \url{https://doi.org/10.1111/2041-210x.70313}
}
