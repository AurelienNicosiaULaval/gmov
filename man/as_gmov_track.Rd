% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coerce.R
\name{as_gmov_track}
\alias{as_gmov_track}
\title{Coerce an object to a gmov track}
\usage{
as_gmov_track(x, x_col = NULL, y_col = NULL, ...)
}
\arguments{
\item{x}{A track-like object. Supported inputs include data frames with
\code{x_} and \code{y_} columns, data frames with \code{x} and \code{y} columns, \code{amt}-style
step objects with \code{x1_}, \code{y1_}, \code{x2_}, and \code{y2_}, and \code{sf} point objects.
Step objects are converted to the corresponding sequence of segment
endpoints.}

\item{x_col, y_col}{Optional character names of the coordinate columns.}

\item{...}{Currently unused.}
}
\value{
A tibble with at least \code{x_} and \code{y_} columns.
}
\description{
Converts a track-like object to the minimal track representation used by
gmov. The returned object is a tibble with numeric \code{x_} and \code{y_} columns.
Many \code{amt} tracks use these coordinate names and should work when they are
data-frame-like. This helper intentionally works with simple object shapes
and does not depend on \code{amt} internals.
Coordinates are assumed to be in a common planar coordinate system. gmov does
not transform track coordinates.
}
\examples{
trk <- data.frame(x = c(0, 1, 2), y = c(0, 1, 1))
as_gmov_track(trk)
}
