% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_ssf_generative}
\alias{validate_ssf_generative}
\title{Validate generative behavior of fitted SSF or iSSF models}
\usage{
validate_ssf_generative(
  observed,
  simulated,
  metrics = c("ud", "msd", "sinuosity", "barrier"),
  barrier = NULL,
  ud_args = list(),
  msd_args = list(),
  sinuosity_args = list(),
  barrier_args = list(),
  ...
)
}
\arguments{
\item{observed}{Observed track-like object compatible with \code{\link[=as_gmov_track]{as_gmov_track()}}.}

\item{simulated}{A list of simulated track-like objects, or a data frame with
one row per simulated location and a simulation identifier column.}

\item{metrics}{Character vector of metrics to compute. Supported values are
\code{"ud"}, \code{"msd"}, \code{"sinuosity"}, and \code{"barrier"}.}

\item{barrier}{Optional \code{sf} LINESTRING or MULTILINESTRING object used for
barrier crossing validation. It is assumed to use the same planar
coordinate system as the tracks.}

\item{ud_args, msd_args, sinuosity_args, barrier_args}{Lists of additional
arguments passed to the corresponding metric functions.}

\item{...}{Passed to coercion helpers.}
}
\value{
An object of class \code{c("gmov_generative", "list")} containing the
function call, track summaries, metric names, metric results, and settings.
}
\description{
Runs a set of trajectory-level generative validation diagnostics on an
observed track and simulated tracks. The fitted model is not required by this
function: users fit and simulate from SSF or iSSF models with tools such as
\code{amt}, then pass the observed and simulated tracks to gmov.
}
\examples{
observed <- data.frame(x = cumsum(c(0, 1, 1, 0)), y = c(0, 0, 1, 1))
simulated <- list(
  data.frame(x = cumsum(c(0, 1, 1, 1)), y = c(0, 0, 0, 0)),
  data.frame(x = cumsum(c(0, 0, 1, 1)), y = c(0, 1, 1, 1))
)

res <- validate_ssf_generative(
  observed = observed,
  simulated = simulated,
  metrics = c("msd", "sinuosity")
)
summary(res)
}
\references{
Nicosia, A. (2026). Beyond the next step: A multi-criteria generative
validation framework for step selection functions. \emph{Methods in Ecology and
Evolution}. \url{https://doi.org/10.1111/2041-210x.70313}
}
