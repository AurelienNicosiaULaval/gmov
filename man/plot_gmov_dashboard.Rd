% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot_gmov_dashboard}
\alias{plot_gmov_dashboard}
\title{Plot an article-style generative validation dashboard}
\usage{
plot_gmov_dashboard(
  x,
  observed = NULL,
  simulated = NULL,
  n_simulations = 25,
  ...
)
}
\arguments{
\item{x}{An object returned by \code{\link[=validate_ssf_generative]{validate_ssf_generative()}}.}

\item{observed}{Optional observed track-like object. If supplied with
\code{simulated}, a trajectory panel is included.}

\item{simulated}{Optional simulated tracks, in any format supported by
\code{\link[=as_gmov_simulations]{as_gmov_simulations()}}. If supplied with \code{observed}, a trajectory panel is
included.}

\item{n_simulations}{Maximum number of simulated tracks shown in the
trajectory panel.}

\item{...}{Passed to \code{\link[=as_gmov_track]{as_gmov_track()}} and \code{\link[=as_gmov_simulations]{as_gmov_simulations()}} when
building the trajectory panel.}
}
\value{
Invisibly returns a named list of ggplot objects after drawing the
dashboard.
}
\description{
Draws a compact multi-panel dashboard for a \code{gmov_generative} result. When
observed and simulated tracks are supplied, the first panel shows observed
and simulated trajectories. The remaining panels show the available
diagnostic plots using the same plotting methods as \code{\link[=plot.gmov_generative]{plot.gmov_generative()}}.
This is a visualization helper only; it does not compute additional
validation metrics.
}
\examples{
observed <- data.frame(x = c(0, 1, 1, 2), y = c(0, 0, 1, 1))
simulated <- list(
  data.frame(x = c(0, 1, 2, 3), y = c(0, 0, 0, 0)),
  data.frame(x = c(0, 0, 1, 1), y = c(0, 1, 1, 2)),
  data.frame(x = c(0, 1, 1, 1), y = c(0, 0, 1, 2))
)
res <- validate_ssf_generative(
  observed,
  simulated,
  metrics = c("msd", "sinuosity"),
  msd_args = list(max_lag = 2)
)
plot_gmov_dashboard(res, observed, simulated)
}
